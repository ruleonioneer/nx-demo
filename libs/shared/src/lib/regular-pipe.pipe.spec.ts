import { RegularPipePipe } from './regular-pipe.pipe';

describe('RegularPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RegularPipePipe();
    expect(pipe).toBeTruthy();
  });
});
